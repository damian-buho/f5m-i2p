---
# SPDX-FileCopyrightText: 2026 Damián Búho <damian.buho@proton.me>
#
# SPDX-License-Identifier: MIT

# .grype.yaml — generated by `pf-bridge vulnerabilities`
# Source: org.projectfile.vulnerabilities (5 suppressed IDs)
# Edit projectfile [org.projectfile.vulnerabilities] suppress to customise —
# direct edits here are overwritten on the next generation.
# pf-cli-managed: yes

ignore:
  - vulnerability: CVE-2026-10050  # unfixable upstream dependency; fixable only via upstream release
  - vulnerability: CVE-2026-1605  # unfixable upstream dependency; fixable only via upstream release
  - vulnerability: CVE-2026-2332  # unfixable upstream dependency; fixable only via upstream release
  - vulnerability: GHSA-355h-qmc2-wpwf  # unfixable upstream dependency; fixable only via upstream release
  - vulnerability: GHSA-xxh7-fcf3-rj7f  # unfixable upstream dependency; fixable only via upstream release
